/**
    UTN
    Tecnicatura Universitaria en Programacion
    Programacion 1 y Laboratorio de computacion 1
    Comision 10
    Prof. Matias Pascual
*/

/**
    CLASE 15:
        MATRICES
            * Declaracion
            * Utilizacion
            * Recorriendo una matriz

        STRING (cadena de caracteres)
            * Utilizacion
            * Principales funciones para el manejo de strings

        MATRICES y STRINGS
            * Entendiendo los arreglos de strings
*/


#include <stdio.h>
#include <stdlib.h>

int ingresarEnPila(Pila* pila)
{
    int numero;
    int cantidadElementos = 0;

    char salir = '0';    
    while(salir != 's')        
    {
        printf("ingrese un numero \n");    
        scanf("%d", &numero);
        
        apilar(pila,numero); 
        
        cantidadElementos++;        
        
        /// para salir...       
    }
    
    return cantidadElementos;    
}

int main()
{
    Pila pila;
    inicpila(&pila);
    
    int cantidadElementosPila = 0;
    
    leer(&pila);
    mostrar(&pila);
    apilar(&pila, 10);
    
    cantidadElementosPila += ingresarEnPila(&pila);

    //ejemplo_matriz();
    //ejemplo_string();
    //ejemplo_operacionesConString();
    //ejemplo_arreglosString();

    return 0;
}

